[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imperandi"
dynamic = ["version"]
description = "IMaging PREprocessing And Normalization for Diagnostic Interoperability"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "scipy",
  "pandas",
  "pyarrow>=14",
  "pydantic>=2",
  "PyYAML>=6",
  "pandarallel",
  "python-dateutil",
  "unidecode",
  "tqdm",
  "pydicom",
  "dicom2nifti",
  "nibabel",
]

[project.optional-dependencies]
imaging = [
  "matplotlib",
  "TotalSegmentator",
  "torchio",
  "scipy",
  "scikit-image",
  "xgboost",
  "SimpleITK"
]

radiomics = [
  "pywavelets",
  "SimpleITK",
  "pyradiomics@git+https://github.com/radiomics/pyradiomics.git",   # because 3.1.0 sdist metadata is broken on PyPI right now
]

qc = [
  "matplotlib",
  "ipympl",
  "ipywidgets",
  "panel",
  "param",
]

dev = [
  "pytest",
  "pytest-cov",
  "black[jupyter]",
  "ruff",
  "ipykernel",
  "ipympl",
  "ipywidgets"
]

all = ["imperandi[dev,imaging,qc,radiomics]"]

[project.scripts]
imperandi = "imperandi.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"imperandi.config" = ["profiles/*.yaml"]

[tool.setuptools.dynamic]
version = {attr = "imperandi._version.__version__"}

[tool.pytest.ini_options]
markers = [
  "slow: IRCAD dataset integration tests",
]

[tool.ruff]
extend-exclude = ["*.ipynb", "**/*legacy*"]
target-version = "py310"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.black]
target-version = ["py310"]
